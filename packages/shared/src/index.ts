/**
 * Shared package.
 *
 * Code that is genuinely reused across multiple apps/packages lives here
 * (shared types, constants, pure helpers). Keep it free of Discord- or
 * bot-specific runtime concerns.
 */

export {};
